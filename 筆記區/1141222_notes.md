# 1141222 Dart/Flutter 課程筆記

## 目錄
1. [字串處理](#字串處理)
2. [變數與型別](#變數與型別)
3. [集合類型](#集合類型)
4. [空安全 (Null Safety)](#空安全-null-safety)
5. [運算符](#運算符)
6. [控制流程](#控制流程)
7. [例外處理](#例外處理)
8. [函數/方法](#函數方法)
9. [物件導向程式設計 (OOP)](#物件導向程式設計-oop)
10. [建構函數進階](#建構函數進階)
11. [繼承與多型](#繼承與多型)
12. [抽象類別與介面](#抽象類別與介面)
13. [Mixins](#mixins)
14. [泛型 (Generics)](#泛型-generics)
15. [函式庫 (Library)](#函式庫-library)
16. [非同步程式設計](#非同步程式設計)

---

## 字串處理

### 正規表示式 (RegExp)
```dart
字串 s01 = "abc1defg2hijk";
var list1 = s01.split(RegExp(r"\d"));  // 用數字分割字串
```

### StringBuffer 合併字串
```dart
StringBuffer s02 = StringBuffer();
s02.writeln('測試');        // 寫入並換行
s02.writeAll(['x','y','z']); // 寫入多個元素
s02.write(100);              // 寫入單一值
s02.writeCharCode(65);       // 寫入字元碼 (A)
```

### 數字/字串轉換
```dart
var int1 = int.parse("123");           // 字串轉整數
var double1 = double.parse("33.3");    // 字串轉浮點數
String str1 = x.toString();            // 數字轉字串
String str2 = pi.toStringAsFixed(2);   // 保留小數位數
```

### Runes (Unicode 字元)
```dart
String heart = '\u2665';      // ♥
String laugh = '\u{1f600}';   // 😀
String greeting = '\u4f60\u597d';  // 你好
```

---

## 變數與型別

### var vs dynamic vs const vs final

| 關鍵字 | 說明 |
|--------|------|
| `var` | 自動推論型別，一旦確定就不能改變型別 |
| `dynamic` | 動態型別，可隨時改變型別 |
| `const` | 編譯時期常數，必須是常數表達式 |
| `final` | 執行時期常數，只能賦值一次 |

```dart
var pi2 = 3.14159;    // 推論為 double，之後不能賦值字串
dynamic pi3 = 3.14159;
pi3 = "3.14159";      // OK，dynamic 可以改變型別

const pi5 = 3.14159;  // 編譯時期常數
final name = 'Bob';   // 執行時期常數，只能賦值一次
final a1 = getValue(); // final 可接受函數回傳值
// const a2 = getValue(); // 錯誤！const 不行
```

### num 型別
```dart
num pi6 = 3;    // num 可以是 int 或 double
pi6 = 3.14;     // OK
```

---

## 集合類型

### List (有序陣列)
```dart
var list1 = [1, 2, 3];           // List<int>
List list2 = [1, 2, 3];          // List<dynamic>
List<int> list3 = [1, 2, 3];     // 明確宣告
var list4 = List.filled(3, 0);   // 固定長度，初始值 0
var constantList = const [1,2,3]; // 常數列表，不可修改
```

**常用操作：**
```dart
list.add(4);              // 新增元素
list.remove(4);           // 移除元素
list[0] = 55;             // 修改元素
list.contains(6);         // 是否包含
list.reversed;            // 反轉 (回傳 Iterable)
list.sort();              // 排序
list.shuffle();           // 洗牌
list1 + list2;            // 合併列表
list.where((i) => i.isOdd); // 篩選奇數
```

**二維列表：**
```dart
List<List<int>> A = [[1,2,3], [4,5,6]];
print(A[0][1]); // 2
```

### Set (無序集合，不重複)
```dart
var set1 = {1, 2, 3};          // Set<int>
var set2 = {};                  // 注意！這是 Map
var set3 = <int>{};            // 空 Set

set1.add(3);                   // 加入重複元素不會改變長度
set4.difference(set5);         // 差集
set4.intersection(set5);       // 交集
set4.union(set6);              // 聯集
Set.from(set4).toList();       // Set 轉 List
```

### Map (鍵值對)
```dart
var g1 = {'first': 'A', 'second': 'B'};
var g2 = Map();
g2['first'] = 'A';

Map<int, String> constantMap = const {1: 'A', 2: 'B'}; // 常數 Map

// 合併
m1.addAll(m2);

// 遍歷
map.forEach((k, v) => print('Key:$k, value:$v'));
```

### Queue (佇列)
```dart
import 'dart:collection';
Queue q1 = Queue();
q1.addAll([10, 20, 30]);
q1.addFirst(5);
q1.addLast(40);
q1.removeFirst();
```

---

## 空安全 (Null Safety)

### 基本概念
```dart
int x;       // x 不可為空，必須先賦值才能使用
int? y;      // y 可為空
y = null;    // OK

x = y!;      // 使用 ! 斷言 y 不為空
```

### 空值檢查運算符
```dart
// ?. 空感知存取
String? value1;
print(value1?.toLowerCase());  // 如果為空就回傳 null

// ?? 空值合併
var result = user ?? 'root';   // 如果 user 為空就用 'root'

// ??= 空值賦值
k ??= 5;  // 如果 k 為空就賦值 5

// ...? 空感知展開運算符
List<int>? c1;
a = [...a, ...?c1];  // 如果 c1 為空就不展開
```

### late 關鍵字
```dart
class A {
  late int aa;  // 延遲初始化，使用前必須賦值
}
```

---

## 運算符

### 算術運算符
| 運算符 | 說明 |
|--------|------|
| `~/` | 整數除法 |
| `%` | 取餘數 |
| `++` / `--` | 遞增/遞減 |

### 位元運算符
```dart
a & b   // AND
a | b   // OR
a ^ b   // XOR
~a      // NOT
a << b  // 左移
a >> b  // 右移
```

### 條件表達式
```dart
condition ? expr1 : expr2  // 三元運算符
expr1 ?? expr2             // 空值合併
```

### 展開運算符 (Spread)
```dart
var list3 = [0, ...list1];     // 展開 list1
var list4 = [0, ...?nullableList]; // 空感知展開
```

### 串聯運算符 (Cascade)
```dart
var student = Student()
  ..id = 1
  ..name = 'Andy'
  ..greeting();
```

### 型別檢查
```dart
if (s1 is String) { }
if (s1 is! int) { }
String myString = s1 as String;  // 型別轉換
```

---

## 控制流程

### 選擇結構
```dart
// if-else
if (score < 60) {
  print("太差了");
} else if (score < 80) {
  print("還不錯");
} else {
  print("很棒！");
}

// switch
switch (command) {
  case 'OPEN': print("開啟"); break;
  case 'CLOSED': print("關閉"); break;
  default: print('未知');
}
```

### 迴圈結構
```dart
// for 迴圈
for (int x = 0; x < 5; x++) { }

// for-in 迴圈
for (var j in list) { }

// forEach
list.forEach((i) => print(i));

// while
while (k <= 10) { k++; }

// do-while
do { } while (x < 5);

// break / continue
```

### 標準輸入/輸出
```dart
import 'dart:io';
stdout.write('輸入：');
m = int.parse(stdin.readLineSync()!);
```

---

## 例外處理

### try-catch-finally
```dart
try {
  // 可能出錯的程式碼
} catch (e) {
  print(e);
} finally {
  print('結束');
}
```

### 自訂例外
```dart
class AmountException implements Exception {
  static String s1 = '金額必須大於零！';
}

// 使用
if (amount < 0) {
  throw AmountException();
}

// 捕捉特定例外
try {
  withDraw(-200);
} on AmountException {
  print(AmountException.s1);
}
```

---

## 函數/方法

### 基本語法
```dart
int add(int a, int b) {
  return a + b;
}

// 箭頭函數
int add(int a, int b) => a + b;
```

### 可選參數 (Optional Positional)
```dart
p1([a = 0]) => a;    // 有預設值
p3([a]) => a;        // 可能為 null
void p3(int a, int b, [int c = 99]) => print(a + b + c);
```

### 命名參數 (Named Parameters)
```dart
void p4(int a, int b, {int c = 99}) => print(a + b + c);
p4(1, 2, c: 3);

// required 必填
sayHello({required String name}) => print("Hello $name");
```

### 函數作為物件/參數
```dart
var f1 = (int a) => print('$a');
arr.forEach(printNum);

void test(Function foo) {
  foo();
}
test(() => print('Test'));
```

### typedef (函數型別別名)
```dart
typedef Calculate = int Function(int n1, int n2);

int test2(Calculate calc) {
  return calc(20, 30);
}

var result = test2((n1, n2) => n1 + n2);
```

### 遞迴
```dart
int Fact(int n) {
  if (n <= 1) return 1;
  else return n * Fact(n - 1);
}

int Fib(int n) {
  if (n <= 1) return n;
  else return Fib(n - 1) + Fib(n - 2);
}
```

---

## 物件導向程式設計 (OOP)

### 類別基礎
```dart
class Circle {
  final pi = 3.14159;
  int r = 0;
  Circle(int q) { r = q; }
  int area() => r * r;
}

Circle c1 = Circle(5);
print(c1.area());
```

### 私有成員 (底線開頭)
```dart
// car.dart
class Car {
  late int _a;  // 私有成員，只能在同檔案存取

  set setA(v) => _a = v;
  int get getA => _a;
}
```

### Getter / Setter
```dart
class Rect {
  num height, width;
  Rect(this.height, this.width);

  get area => height * width;
  set areaHeight(v) => height = v;
}
```

---

## 建構函數進階

### 命名建構函數
```dart
class Cat {
  late String name, color;

  Cat(this.name, this.color);
  Cat.orange(this.name) { color = '橘色'; }
  Cat.black(this.name) { color = '黑色'; }
  Cat.fromJson(Map<String, dynamic> json) {
    name = json['name'];
    color = json['color'];
  }
}
```

### 初始化列表
```dart
class Point {
  final sum, factor;
  Point(x, y) : sum = x + y, factor = x / y;
}

class Rectangle {
  final int width, height, area;
  Rectangle(this.width, this.height) : area = width * height;
}
```

### 重定向建構函數
```dart
class Point {
  double x, y;
  Point(this.x, this.y);
  Point.alongXAxis(double x) : this(x, 0);  // 重定向
}
```

### 常數建構函數
```dart
class Student {
  final String name;
  final int age;
  const Student(this.name, this.age);
}

const s1 = Student('Ada', 22);
const s2 = Student('Ada', 22);
print(identical(s1, s2));  // true (同一個實例)
```

### 工廠建構函數 (Factory)
```dart
class Person {
  String name;
  static final Map<String, Person> _cache = {};

  Person(this.name);

  factory Person.withName(String name) {
    if (_cache.containsKey(name)) {
      return _cache[name]!;
    } else {
      final p = Person(name);
      _cache[name] = p;
      return p;
    }
  }
}
```

### 單例模式 (Singleton)
```dart
class S {
  static final S s1 = S._internal();
  S._internal();
  factory S() => s1;
}

var ss1 = S();
var ss2 = S();
print(identical(ss1, ss2));  // true
```

---

## 繼承與多型

### 繼承 (extends)
```dart
class Animal {
  void eat() => print('動物進食');
}

class Dog extends Animal {
  @override
  void eat() => print('狗進食');

  void eat1() {
    this.eat();   // 狗進食
    super.eat();  // 動物進食
  }
}
```

### 呼叫父類建構函數
```dart
class Person {
  String name;
  Person(this.name);
}

class Student extends Person {
  int score;
  Student(String name, this.score) : super(name);
}
```

### 運算符重載
```dart
class Vector {
  num x, y;
  Vector(this.x, this.y);

  operator +(Vector v) => Vector(x + v.x, y + v.y);
  operator -(Vector v) => Vector(x - v.x, y - v.y);
}
```

---

## 抽象類別與介面

### 抽象類別
```dart
abstract class Shape {
  int getArea();  // 抽象方法
  String getInfo() => '形狀';  // 具體方法
}

class Rectangle extends Shape {
  @override
  getArea() => 100;
}

// final s = Shape();  // 錯誤！不能實例化抽象類別
```

### 密封類別 (Sealed Class)
```dart
sealed class SealedStatus {
  void display() {}
}

class Playing extends SealedStatus {
  @override
  void display() => print('播放中');
}
// 密封類別只能在同一檔案中被繼承
```

### 介面 (implements)
```dart
abstract class A { fnA(); }
abstract class B { fnB(); }

class C implements A, B {
  @override
  fnA() => print('A');
  @override
  fnB() => print('B');
}
```

---

## Mixins

### 基本用法
```dart
mixin Swim {
  void swim() => print('游泳');
}

mixin Fly {
  void fly() => print('飛行');
}

class Duck extends Bird with Swim, Fly { }
```

### mixin on (限制)
```dart
abstract class Reptile with Swim, Bite, Crawl { }

mixin Hunt on Reptile {  // Hunt 只能用於 Reptile 的子類
  void hunt(String food) {
    swim();
    crawl();
    bite();
  }
}

class Alligator extends Reptile with Hunt { }
```

### 類別別名
```dart
class C = A with B;  // C 繼承 A 並混入 B
```

---

## 泛型 (Generics)

### 泛型類別
```dart
class Pair<T> {
  T x, y;
  Pair(this.x, this.y);
}

var p1 = Pair<int>(10, 20);
var p2 = Pair<String>('A', 'B');
```

### 泛型約束
```dart
class Point<T extends num> {
  T x, y;
  Point(this.x, this.y);
}
```

### 泛型方法
```dart
T printFirst<T>(List<T> lst) {
  T first = lst[0];
  print(first);
  return first;
}
```

---

## 函式庫 (Library)

### 系統函式庫
```dart
import 'dart:math';
import 'dart:io';
import 'dart:collection';
```

### 自訂函式庫
```dart
// util.dart
int add(int a, int b) => a + b;

// main.dart
import './util.dart';
import './util.dart' as myUtil;  // 別名
import './util.dart' show add;   // 只匯入 add
import './util.dart' hide add;   // 隱藏 add
```

### 匯出多個檔案
```dart
// utils.dart
export 'math1.dart';
export 'math2.dart';
```

---

## 非同步程式設計

### async / await
```dart
Future<String> getData() async {
  var response = await http.get(Uri.parse(url));
  return response.body;
}

void main() async {
  var data = await getData();
  print(data);
}
```

### Future
```dart
// Future.value
Future<int> futureWait() => Future.value(10);

// Future.delayed
Future<String> futureDelayed() {
  return Future.delayed(Duration(seconds: 3), () => '延遲 3 秒');
}

// Future.error
Future<String> futureError() => Future.error('錯誤訊息');

// 使用
futureWait()
  .then((i) => print(i))
  .catchError((e) => print(e))
  .whenComplete(() => print('完成'));
```

### Stream
```dart
import 'dart:async';

// 單一訂閱者
StreamController<String> controller = StreamController<String>();
controller.stream.listen(
  (data) => print(data),
  onError: (e) => print(e),
  onDone: () => print('串流結束'),
);
controller.sink.add('資料');
controller.close();

// 廣播 (多訂閱者)
StreamController<String> controller = StreamController<String>.broadcast();

// await for
Future<int> sumStream(Stream<int> stream) async {
  var sum = 0;
  await for (var i in stream) {
    sum += i;
  }
  return sum;
}
```

### Stream 建構方式
```dart
// 從 Future
Stream.fromFuture(Future.delayed(Duration(seconds: 1), () => 10));

// 從多個 Future
Stream.fromFutures(listOfFutures);

// 從 Iterable
Stream.fromIterable([1, 2, 3, 4, 5]);
```

---

## Flutter 相關範例

### StatelessWidget vs StatefulWidget
```dart
// 無狀態
class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(...);
  }
}

// 有狀態
class MyHomePage extends StatefulWidget {
  @override
  State<MyHomePage> createState() => _MyHomePageState();
}

class _MyHomePageState extends State<MyHomePage> {
  int _counter = 0;

  void _incrementCounter() {
    setState(() {
      _counter++;
    });
  }

  @override
  Widget build(BuildContext context) { ... }
}
```

### StreamBuilder
```dart
StreamBuilder(
  stream: controller.stream,
  builder: (context, snapshot) {
    if (snapshot.hasData) {
      return Text('${snapshot.data}');
    }
    return CircularProgressIndicator();
  },
)
```

---

## 重點整理

1. **空安全**：善用 `?`, `!`, `??`, `?.` 處理可能為 null 的值
2. **const vs final**：const 是編譯期常數，final 是執行期常數
3. **List/Set/Map**：了解三種集合的特性與使用時機
4. **OOP 四大特性**：封裝、繼承、多型、抽象
5. **Mixins**：實現程式碼重用，避免多重繼承問題
6. **泛型**：提供型別安全的程式設計方式
7. **非同步**：使用 Future/Stream 處理非同步操作
